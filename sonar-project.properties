# Required metadata
sonar.projectKey=locahost:mps-unit-tests
sonar.projectName=MPS analyzed with the SonarQube Runner
sonar.projectVersion=1.0

# Path to the parent source code directory.
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=spec

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8

# Optional
sonar.tests=tests

# To import the JS Sonar report
sonar.javascript.jstestdriver.reportsPath=reports
#sonar.javascript.lcov.reportPath=reports/lcov.dat

